# 23种设计模式分类

```
创建型（5种）：
    类模型：
        工厂模式；
    对象模型：
        抽象工厂；
        单例；
        建造者；
        原型；
结构型（7种）：
    类模型：
        适配器；
    对象模型：
        适配器；
        代理；
        组合；
        外观；
        装饰；
        桥接；
        享元；
行为型（11种）：
    类模型：
        解释器；
        模板方法；
    对象模型：
        访问者；
        状态；
        命令；
        备忘录；
        责任链；
        中介者；
        观察者；
        策略；
        迭代器；
```

# 时钟周期

```
时钟周期：
    是处理操作的最基本单位；
    通常称为 节拍脉冲或 T 周期；
```

# 机器周期

```
机器周期：
    也称为 CPU 周期；
    一条指令的执行过程划分为若干个阶段（如取指、译码、执行等），每一个阶段完成一个基本操作，完成一个基本操作所需要的时间称为机器周期；
    常用从内存中读取一个指令字的最短时间来规定 CPU 周期；
    一个机器周期由若干个状态周期组成；
```

# 指令周期

```
指令周期：
    执行一条指令所需要的时间，一般由若干个机器周期组成；
    指令不同，所需的机器周期数也不同，常常用若干个 CPU 周期数来表示；
    一条指令的取出阶段需要一个 CPU 周期时间，而一个 CPU 周期时间又包含若干个时钟周期，这些时钟周期的总和，决定了一个 CPU 周期的时间宽度；
    取出和执行任何一条指令所需的最短时间为两个 CPU 周期；
    对于复杂一些的指令，则需要更多的 CPU 周期；
```

# 存储周期

```
存储周期：
    指主存储器两次启动操作之间需要的最小时间间隔，也称之为主存储器周期时间，稍长于主存读写存取周期；
    CPU 通过使用AR（地址寄存器）、DR（数据缓冲寄存器）、总线与主存进行数据传送；
    连续完成两次读/写操作中间需待前一操作信号全部消失后才能进行下一个操作，故半导体存储器的周期时间通常稍大于其存取时间，而磁芯存储器的周期时间通常是其存取时间的两倍；
```

# 总线周期

```
总线周期：
    主存和I/O端口是挂接在总线上的，CPU对主存和I/O接口的访问是通过总线实现的；
    CPU 通过总线对微处理器外部的主存或I/O接口进行一次访问所需的时间成为一个总线周期；
    一个总线周期一般包含 4 个时钟周期，分别称为 4 个状态（T1状态、T2状态、T3状态、T4状态）；
```

# 大根堆

```
1、将数据按照顺序构建一颗完全二叉树；
2、从最后一个非终端节点开始构建，把三个值当中最大的放在根的位置；
3、找下一个非终端节点进行同样的操作；
4、直至找到整个二叉树的根节点；
5、再回来查询有没有由于上层的交换最大值的操作造成下层已经交换过的根的大小；

2,8,7,1,3,5,6,4；
第一步：构造完全二叉树
                  2
           |--------------|
           8              7
      |---------|    |---------|
      1         3    5         6
   |---
   4

第二步：首先找到最后一个非终端节点，1比4小，交换位置
                  2
           |--------------|
           8              7
      |---------|    |---------|
      4         3    5         6
   |---
   1
   
第三步：找到7节点，7都比5、6大，不交换位置
                  2
           |--------------|
           8              7
      |---------|    |---------|
      4         3    5         6
   |---
   1   

第四步：找到8节点，8都比4、3大，不交换位置
                  2
           |--------------|
           8              7
      |---------|    |---------|
      4         3    5         6
   |---
   1  
   
第五步：找到2节点，8都比2大，交换2、8位置
                  2
           |--------------|
           8              7
      |---------|    |---------|
      4         3    5         6
   |---
   1   
第6步：回到2节点的位置，2比3、4都小，交换2、最大值的位置
                  8
           |--------------|
           2              7
      |---------|    |---------|
      4         3    5         6
   |---
   1   
   
第7步：所有非终端节点都已经满足大根堆条件
                  8
           |--------------|
           4              7
      |---------|    |---------|
      2         3    5         6
   |---
   1   

    
```

# 算法经典例子

## 最大字段和

```
时间复杂度：O(nlogn)
```

# 算法设计策略

## 动态规划

```
最优子结构
重叠子问题
自底向上
```

## 回溯算法

```
以深度优先方法搜索解空间
```

## 分支限界法

```
以广度优先方法搜索解空间
```

# 2018年

## 上半年

```
(12)
非法进行拷贝、发布或更改软件的人被称为软件盗版者；

《计算机软件保护条例》是 国务院 颁布的，用来保护软件著作权人的权益
```

```
(14)
著作权中，署名权、修改权、保护作品完整权 的保护期不受限制；
```

```
(15)
数据字典包括 数据流、数据项、基本加工，不包括外部实体；
```

```
(19)
COCOMO II 使用的 3 个估算选择对象：对象点、功能点、代码行；
```

```
(20)
Lisp 函数式语言
Prolog 逻辑程序设计语言，以形式逻辑为基础
Phthon、SmallTalk 面向对象语言
```

```
(29)
增量模型（瀑布模型 + 原型模型）：
容易理解、管理成本低（是整个瀑布模型的优点）
核心产品往往首先开发，因此经历最充分的“测试”
第一个可交付版本所需要的成本低、时间少
用户需求不清晰，对进度和质量有影响，同样也是瀑布模型的缺点
```

```
(30)
CMMI成熟度模型
1.阶段式模型
阶段式模型的结构类似于 CMM,它关注组织的成熟度。CMMI有5个成熟度等级:
    初始的:过程不可预测且缺乏控制。
    已管理的:过程为项目服务。
    已定义的:过程为组织服务。
    定量管理的:过程已度量和控制。
    优化的:集中于过程改进。
2.连续式模型
连续式模型关注每个过程域的能力，一个组织对不同的过程域可以达到不同的过程域能力等级(Capability Leve, CL)。CMMI中包括6个过程域能力等级，能力等级可以独立地应用于任何单独的过程域，任何一个能力等级都必须满足比它等级低的能力等级的所有准则。对各能力等级的含义简述如下:
    CLO(未完成的):过程域未执行或未得到CL1中定义的所有目标。
    CL1(已执行的):其共性目标是过程将可标识的输入工作产品转换成可标识的输出工作产品，以买现支持过程域的特定目标。（本答案）
    CL2(已管理的):其共性目标集中于已管理的过程的制度化。根据组织级政策规定过程的运作将使用哪个过程，项目遵循文档化的计划和过程描述，所有正在工作的人都有权使用足够的资源，所有工作任务和工作产品都被监控、控制和评审。
    CL3(己定义级的):其共性目标集中于已定义的过程的制度化。过程是按照组织的剪裁指南从组织的标准过程集中剪裁得到的，还必须收集过程资产和过程的度量，并用于将来对过程的改进。
    CL4(定量管理的):其共性目标集中于可定量管理的过程的制度化。使用测量和质量保证来控制和改进过程域,建立和使用关于质量和过程执行的定量目标作为管理准则。
    CL5(优化的):使用量化(统计学)手段改变和优化过程域，以满足客户要求的改变和持续改进计划中的过程域的功效。 
```

```
(31)
软件维护工具：版本控制、文档分析、开发信息库、逆向工程、再工程；不包含软件配置管理；
软件配置管理是一种过程管理类别，标识变更、控制变更、确保变更正确实现、报告有关变更的相关活动；
```

```
(32)
概要设计：总体结构设计、数据结构和数据库设计、概要文档、概要设计评审；
详细设计：模块详细算法设计、模块内数据结构设计、数据库物理设计、其它设计、详细文档、详细设计评审；
```

```
(33)
模块耦合程度取决于：模块间接口复杂度、调用模块的方式、通过接口的信息类型；
```

```
(45)
桥接模式属于 结构性对象设计模式
```

```
(50)
语法制导翻译是一种静态语义分析方法；
```

```
(55)
数据库
1、标量值函数：
create function 函数名（@参数名称 参数数据类型）
    return integer # 定义函数返回值类型
declare 变量名 变量类型 # declare是声明的意思
如：declare d_counter integer
begin
    函数体
    return 变量名
end
end

在mysql中，变量名之前不需要加“@”，在sqlserver中变量名之前需要加“@”

2、表格值函数：
create function 函数名（@参数名称 参数数据类型）
    return table # 定义函数返回值类型-表格
declare 变量名 变量类型 # declare是声明的意思
如：declare d_counter integer
begin
    函数体
    return 一条Sql语句
end
end
```

```
(56)
某集团公司下属有多个超市，每个超市的所有销售数据最终要存入公司的数据仓库中。假设该公司高管需要从时间、地区和商品种类三个维度来分析某家电商品的销售数据，那么最适合采用 OLAP 来完成。

ETL工具（集成工具）：用于数据抽取（Extracting）、清洗（Cleaning）、转换（Transformation）、加载（Load）的工具；

OLAP（Online Analytical Processing 练级分析处理）服务器：对分析所需要的数据按照多维数据模型进行再次组织，以支持用户多角度、多层次的数据分析；

OLTP（Online Transaction Processing 联机事务处理）：利用计算机网络，将分布于不同地理位置的业务处理计算机设备或网络与业务管理中心网络连接，以便于在任何一个网络节点上都可以进行统一、实时的业务处理活动或客户服务；
```

```
(57)
用循环单链表表示队列，则入队、出队操作都不需要遍历链表
```

```
(59)
用数组顺序存储二叉树，空的节点也是需要占位置的
```

```
(61)
图的遍历是按照某种策略访问图中的每一个顶点，是通过边或弧找邻接点的过程，不论广度搜索还是深度搜索，时间复杂度都一样。邻接矩阵遍历图时间复杂度为 O(n^2)，邻接表遍历时间复杂度为O(n+e)。
```

```
(66)
网络设备
物理层：中继器、集线器；
数据链路层：网桥、交换机；
网络层：路由器
```

```
(69)
更改文件权限使用命令：
    Linux：chmod;
    Windows：attrib;
```

```
(70)
主域名服务器在接收到域名请求时：
    域名服务器上的高速缓存 ------> 查询当前服务器的解析数据文件内容（针对本地域名）或者向其它服务器发送查询请求（针对非本地域名）
```

## 下半年

```
(5)
奇偶校验只能检错，不能纠错；
若有奇数个数据位出错，则可以检测出该错误，但无法纠错；

一位奇校验方法，即在原始数据中加入1bit的校验位，使得总数据中1的个数为奇数个；
一位偶校验方法，即在原始数据中加入1bit的校验位，使得总数据中1的个数为偶数个；
```

```
(9)
文字摘要算法
MD5 512 位分组，128位
SHA 512 位分组，160位
```

```
(11)
使用邮件客户端，需要自己手动配置SMTP（简单邮箱传输协议）以实现邮件的发送，手动配置POP3协议以实现邮件的接收；
通过web方式时，则不需要再配置协议，因为对应的邮件供应商已经配置好了；
无论哪种方式，都是需要账号密码登录的
```

```
(13)
《中华人民共和国著作权法》、《计算机软件保护条例》是构成我国保护计算机软件著作权的两个基本法律文件；
《计算机软件保护条例》是 国务院 颁布的，用来保护软件著作权人的权益
```

```
(15)
结构化分析的输出有：数据流图，数据字典、加工逻辑；不包含结构图；
结构图是结构化设计阶段的产物；
```

```
(30)
CMMI成熟度模型
1.阶段式模型
阶段式模型的结构类似于 CMM,它关注组织的成熟度。CMMI有5个成熟度等级:
    初始的:过程不可预测且缺乏控制。
    已管理的:过程为项目服务。
    已定义的:过程为组织服务。
    定量管理的:过程已度量和控制。
    优化的:集中于过程改进。
2.连续式模型
连续式模型关注每个过程域的能力，一个组织对不同的过程域可以达到不同的过程域能力等级(Capability Leve, CL)。CMMI中包括6个过程域能力等级，能力等级可以独立地应用于任何单独的过程域，任何一个能力等级都必须满足比它等级低的能力等级的所有准则。对各能力等级的含义简述如下:
    CLO(未完成的):过程域未执行或未得到CL1中定义的所有目标。
    CL1(已执行的):其共性目标是过程将可标识的输入工作产品转换成可标识的输出工作产品，以买现支持过程域的特定目标。（本答案）
    CL2(已管理的):其共性目标集中于已管理的过程的制度化。根据组织级政策规定过程的运作将使用哪个过程，项目遵循文档化的计划和过程描述，所有正在工作的人都有权使用足够的资源，所有工作任务和工作产品都被监控、控制和评审。
    CL3(己定义级的):其共性目标集中于已定义的过程的制度化。过程是按照组织的剪裁指南从组织的标准过程集中剪裁得到的，还必须收集过程资产和过程的度量，并用于将来对过程的改进。
    CL4(定量管理的):其共性目标集中于可定量管理的过程的制度化。使用测量和质量保证来控制和改进过程域,建立和使用关于质量和过程执行的定量目标作为管理准则。
    CL5(优化的):使用量化(统计学)手段改变和优化过程域，以满足客户要求的改变和持续改进计划中的过程域的功效。 
```

```
(31)
功能性：
    适合性；
    准确性；
    互用性；
    依从性；
    安全性；
可靠性：
    成熟性；
    容错性；
    易恢复性；
易使用性：
    易理解性；
    易学性；
    易操作性；
效率：
    时间特性；
    资源特性；
可维护性：
    易分析性；
    易改变性；
    稳定性；
    易测试性；
可移植性：
    适应性；
    易安装性；
    一致性；
    易替换性；
```

```
(41)
构件图展现了一组构件之间的组织和依赖关系；
左半圆 '('表示需接口；
一个箭头对一个圆'--->○---'表示供接口；
```

```
(45)
访问者模式（Visitor）表示一个作用于某对象结构中各元素的操作。可以在不改变各元素的类的前提下定义作用于这些元素的新操作；
访问者模式是行为型对象模式，需要对一个对象结构中的对象进行很多不同的并且不相关的操作；
在23种设计模式中，只有中介者模式和访问者模式的代码中含有accpet方法；
```

```
(51)
数据库三级两映射：外模式、模式、内模式；
模式即概念模式、一个数据库只有一个概念模式，对应数据库中的基本表；
一个数据库可以有多个外模式，但一个应用只能使用一个外模式；
一个数据库只有一个内模式，对应存储文件；
```

```
(52)
位置透明：
    用户看到的是全局数据模型的描述，不需要考虑数据的存储场地和操作的执行场地；

分片透明：
    分片过程是一个关系分成几个子关系，每个子关系成为一个分片，根据需求，一个分片可能存储在不同的场地上，用户无需考虑数据分片的细节，应用程序对分片的调用由系统自动完成；

复制透明：
    数据可重复存储在不同的场地上，从而提高系统的可靠性和可用性，用户只看到单一的数据副本，由系统复杂对冗余数据一致性等的控制和维护；
```

```
(57)
用单链表作为栈的存储结构，并用头指针作为栈顶指针，则入栈和出栈都不需要遍历链表；
```

```
(59)
构建过程：从无到有、一步一步地将后续的数依次插入到已构建的二叉排序树中的合适位置；
选项B：23 40 91 17 19 10 31 65 27 13
第一步：
                23
		
第二步：	
                23
                 --------|
                         40
第三步：	
                23
                 --------|
                         40
                          ---------|
                                   91
第四步：	
                23
        |----------------|
        17               40
                          ---------|
                                   91
第五步：	
                23
        |----------------|
        17               40
         ------|          ---------|
               19                  91
第六步：	
                23
        |----------------|
        17               40
    |-------|             ---------|
   10       19                     91
第七步：	
                23
        |----------------|
        17               40
     |------|	   |------------|
     10     19     31           91
第八步：	
                23
        |----------------|
        17               40
     |------|	   |------------|
     10     19     31           91
                           |-----
                           65
第九步：	
                23
        |----------------|
        17               40
     |------|	   |------------|
     10     19     31           91
               |----       |-----
               27          65
第十步：	
                23
        |----------------|
        17               40
     |------|	   |------------|
     10     19     31           91
      ---|     |---          |---
         13    27            65
```

```
(66)
使用ADSL接入Internet，用户端需要安装 PPPoE协议；
PPPoE是利用以太网发送PPP并且支持在同一以太网上建立多个PPP连接的接入技术。其结合了以太网和PPP连接的综合属性。在ADSL拨号中，经常应用PPPoE，用于向广大普通用户提供认证、计费服务，也可用于固定用户申请独立的一个公网IP地址。PPPoE认证的主要特点在于其应用于广泛、成熟；而且标准性、互通性好。
```

